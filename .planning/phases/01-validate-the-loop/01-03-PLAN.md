---
phase: 01-validate-the-loop
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - shared/photo.js
  - maker.html
  - docs/Stage-0-Decision-Ledger.md
autonomous: false
requirements: [VAL-03, VAL-04]
user_setup:
  - service: google-sheets
    why: "The generation-tree ledger is the source of truth for verified organic propagation (D-03 specifics). PostHog only corroborates. The decision is read mechanically from this ledger."
    dashboard_config:
      - task: "Create a Google Sheet from the column schema in docs/Stage-0-Decision-Ledger.md, then paste the pre-committed thresholds into the header and timestamp it BEFORE card #1 ships"
        location: "Google Sheets -> new sheet"

must_haves:
  truths:
    - "The maker validates required fields inline on submit (name, note, consent) with the UI-SPEC error copy, and the consent checkbox is required to enable submit"
    - "A photo is downscaled client-side to a JPEG under ~1600px before upload, and an undecodable photo (e.g. iPhone HEIC on Android Chrome) shows the UI-SPEC error copy instead of a blank upload"
    - "The share state shows the /c/<token>/ link with a working Copy link button (transient Copied feedback) and a scannable QR for the same link"
    - "A pre-committed decision ledger doc exists with the D-03 KILL/ITERATE/CONTINUE thresholds, the column schema, the verified-propagation definition, and the ~30-day / min-10-engaged window, written to be timestamped before card #1"
    - "Both cohorts (couples + college) and the consent/who-sent-you fields are accounted for so VAL-04 can run"
  artifacts:
    - path: "shared/photo.js"
      provides: "downscalePhoto(file) -> JPEG blob under ~1600px via createImageBitmap/canvas.toBlob, with a decode-failure path"
      exports: ["downscalePhoto"]
    - path: "maker.html"
      provides: "Surface A validation/error states + consent + who-sent-you field, Surface B QR + copy-link feedback + Make another, photo preview + remove"
      contains: "Copy link"
    - path: "docs/Stage-0-Decision-Ledger.md"
      provides: "the pre-committed decision rule, ledger column schema, verified-propagation definition, and the cohort plan (VAL-04)"
      contains: "CONTINUE"
  key_links:
    - from: "maker.html"
      to: "shared/photo.js"
      via: "downscalePhoto(file) runs before saveCard uploads the blob"
      pattern: "downscalePhoto"
    - from: "maker.html share state"
      to: "qrcode"
      via: "QRCode.toCanvas/toDataURL renders the /c/<token>/ link"
      pattern: "QRCode"
    - from: "docs/Stage-0-Decision-Ledger.md"
      to: "the PostHog funnel"
      via: "the ledger is the source of truth; PostHog event names corroborate (named in 01-02-SUMMARY)"
      pattern: "propagation"
---

## Phase Goal

**As a** sender in the controlled cohort, **I want to** a maker that handles my photo and mistakes gracefully and hands me a clean shareable link with a QR, **so that** I can actually send the card, and **as the** founder **I want** a pre-committed ledger so the go/no-go is read mechanically, not rationalized after the fact.

<objective>
Harden the maker into something the cohort can really use, and author the VAL-04 decision instrument. Add client-side photo downscale + HEIC-failure handling (research Pitfall 3), inline validation and the UI-SPEC error states, the required consent checkbox and the optional who-sent-you field, and the Surface B share state with a QR code and copy-link feedback. Then write the pre-committed `docs/Stage-0-Decision-Ledger.md` carrying the D-03 KILL/ITERATE/CONTINUE thresholds, the generation-tree column schema, the verified-organic-propagation definition, and the ~30-day / minimum-10-engaged window, structured so the founder timestamps the thresholds before card #1.

This plan depends on Plan 01 (`maker.html`, `shared/supabase.js`) and Plan 02 (the PostHog event names recorded in `01-02-SUMMARY.md`).

Purpose: Plan 01 proved the loop; Plan 02 made it measurable; this plan makes it usable by real testers and makes the go/no-go mechanical. Without the ledger there is no honest decision (the whole point of the GATE).
Output: `shared/photo.js`, the hardened `maker.html`, and `docs/Stage-0-Decision-Ledger.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-validate-the-loop/01-CONTEXT.md
@.planning/phases/01-validate-the-loop/01-RESEARCH.md
@.planning/phases/01-validate-the-loop/01-UI-SPEC.md
@.planning/phases/01-validate-the-loop/SKELETON.md
@.planning/phases/01-validate-the-loop/01-01-SUMMARY.md
@.planning/phases/01-validate-the-loop/01-02-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/REQUIREMENTS.md
@index.html
</context>

<interface_context>
Photo handling (research Don't Hand-Roll + Pitfall 3, all browser-native, no library):
- Downscale with `createImageBitmap(file)` then draw onto a `<canvas>` sized so the long edge is <= ~1600px, then `canvas.toBlob(cb, 'image/jpeg', 0.8)`.
- HEIC decodes natively only in Safari; on Chrome/Firefox `createImageBitmap` of an iPhone HEIC throws or yields a blank. Wrap in try/catch; on failure return a sentinel/throw the caller catches and shows the UI-SPEC upload error copy and asks for a JPEG. A WASM HEIC decoder is explicitly out of scope.
- The file input should `accept="image/jpeg,image/png,image/webp"` (research Pitfall 3a).

QR (research Standard Stack, major-pinned import): `import QRCode from 'https://esm.sh/qrcode@1'`, then `QRCode.toCanvas(canvasEl, url)` or `toDataURL` for the share-state QR of the `/c/<token>/` link. Fully client-side.

UI-SPEC copy (verbatim, no forbidden long-dash):
- Validation: `Add their first name` / `Write a note first`.
- Upload too large: `That photo's a little big. Try one under 10MB.`
- Upload failed/network: `That didn't go through. Check your connection and try again.`
- Save/link failed: `We couldn't make your link just now. Give it another try.` with a retry affordance.
- Share heading `Your card is ready.`; body `Send this link to their phone. They just tap it.`; `Copy link` -> transient `Copied`; QR caption `Or let them scan this.`; `Make another`; `Preview it`.
- Consent checkbox (required to enable submit): `I have permission to use this photo, and I understand this is an early experiment.` (Stage 0 spec section 5.2).
- Optional field: a who-sent-you-the-card-you-saw text input for soft propagation evidence.

D-03 thresholds for the ledger (verbatim intent): read on a ~30-day rolling window, minimum 10 engaged recipients. KILL = zero verified organic propagation across the cohort OR the finish-to-send rate upper 95% bound below ~15%. ITERATE (one round only) = tripwire alive but <=1 verified propagation. CONTINUE = >=2 independent verified organic-propagation events (2 distinct chains: 2 distinct non-recruited people who each received AND made their own). Verified means a gen-1 requester arrived via a tracked ref to a gen>=1 card AND names/pastes the card sent to them (self-report is soft evidence only). Cohorts: long-distance couples + college students (VAL-04). The thresholds + exact metric definitions are written into the ledger header and timestamped before card #1.

Only `maker.html` is shared with Plans 01/02 (this plan is wave 3 because of that overlap). Do not touch `card.html`/`404.html` here.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Client-side photo downscale with HEIC-failure handling</name>
  <files>shared/photo.js</files>
  <action>
Build `shared/photo.js` exporting `async downscalePhoto(file)`. Use `createImageBitmap(file)` inside a try/catch; compute target dimensions so the long edge is at most a named MAX_EDGE constant (~1600px), preserving aspect ratio; draw onto an offscreen/temporary canvas; `canvas.toBlob` to `image/jpeg` at ~0.8 quality and resolve the resulting Blob. On a decode failure (the HEIC-on-non-Safari case, research Pitfall 3) or a toBlob failure, reject/throw a typed error the caller distinguishes from a network error so the maker can show the right UI-SPEC copy. Also reject early when the original file exceeds the 10MB UI-SPEC cap (so the maker shows the too-big copy). Keep it dependency-free (browser-native only) and free of the forbidden long-dash.
  </action>
  <verify>
    <automated>node --input-type=module -e "import('./shared/photo.js').then(m=>process.exit('downscalePhoto' in m?0:1)).catch(e=>{console.error(String(e));process.exit(1)})"</automated>
    <automated>grep -q "createImageBitmap" shared/photo.js && grep -q "toBlob" shared/photo.js && echo DOWNSCALE_OK</automated>
    <automated>node -e "const fs=require('fs');if(fs.readFileSync('shared/photo.js','utf8').includes('—')){process.exit(1)}console.log('NO_LONGDASH')"</automated>
  </verify>
  <done>`shared/photo.js` exports `downscalePhoto`; it downscales to a JPEG under the max edge, rejects oversized files, and distinguishes a decode failure from other errors; no library import; no forbidden long-dash.</done>
</task>

<task type="auto">
  <name>Task 2: Maker validation, error states, consent, photo preview, and the Surface B share state (QR + copy)</name>
  <files>maker.html</files>
  <action>
Harden `maker.html` on top of the Plan 01/02 skeleton. Add inline validation on submit-attempt (not aggressive on-blur, per UI-SPEC): require recipient first name and the note, and require the consent checkbox to enable submit; show the UI-SPEC error copy in `--rose-deep` under the offending field. Wire `downscalePhoto(file)` from `shared/photo.js` before `saveCard` uploads, and on its decode/oversize/network failures show the matching UI-SPEC error copy (too-big, did-not-go-through, or the save/link-failed copy with a retry affordance). Render the taped-polaroid-style preview thumbnail on select (reuse `.polaroid` styling) with the `x` remove control and the `Photo removed.` caption. Add the required consent checkbox and the optional who-sent-you-the-card-you-saw text input (soft propagation evidence for the ledger).

Build the Surface B share state: heading `Your card is ready.`, body `Send this link to their phone. They just tap it.`, the read-only link field with a `Copy link` button that swaps to `Copied` (rose) for ~1.5s using the Clipboard API, a centered QR for the `/c/<token>/` link via `QRCode` (major-pinned import) with caption `Or let them scan this.`, a `Make another` reset link, and an optional `Preview it` link opening the live card in a new tab. All copy verbatim from the UI-SPEC; reuse the inherited tokens/fonts; no new hex values; no forbidden long-dash.
  </action>
  <verify>
    <automated>grep -q "downscalePhoto" maker.html && grep -q "QRCode" maker.html && grep -q "Copy link" maker.html && echo MAKER_HARDENED</automated>
    <automated>grep -q "permission to use this photo" maker.html && echo CONSENT_PRESENT</automated>
    <automated>node -e "const fs=require('fs');if(fs.readFileSync('maker.html','utf8').includes('—')){process.exit(1)}console.log('NO_LONGDASH')"</automated>
  </verify>
  <done>`maker.html` validates required fields + consent, downscales the photo with graceful HEIC/oversize/network errors, previews + removes the photo, and the share state shows a working Copy link (with Copied feedback) and a scannable QR; no forbidden long-dash.</done>
</task>

<task type="auto">
  <name>Task 3: The pre-committed Stage 0 decision ledger (VAL-04)</name>
  <files>docs/Stage-0-Decision-Ledger.md</files>
  <action>
Write `docs/Stage-0-Decision-Ledger.md` as the pre-committed decision instrument (D-03 / VAL-04). It MUST contain: (1) a header block for the founder to paste the exact thresholds and a timestamp BEFORE card #1 ships (the rule is only valid if committed first); (2) the full KILL / ITERATE / CONTINUE thresholds verbatim from D-03 (zero verified propagation OR finish-to-send upper 95% bound below ~15% = KILL; tripwire alive but <=1 verified propagation = ITERATE, one round; >=2 independent verified organic-propagation events = CONTINUE); (3) the ~30-day rolling window and minimum-10-engaged-recipients reading rule; (4) the verified-organic-propagation definition (arrived via a tracked ref to a gen>=1 card AND names/pastes the card they received; 2 distinct chains; self-report is soft evidence only); (5) the generation-tree column schema for the Google Sheet (e.g. card token, generation, ref/parent token, cohort [couples|college], recipient handle, opened, engaged, cta_clicked, made-their-own, verified-propagation flag, notes) and a note that the sheet is the source of truth and PostHog (event names from 01-02-SUMMARY) corroborates; (6) the two-cohort plan (long-distance couples + college students). Keep it operational and unambiguous so the decision is read mechanically. No forbidden long-dash anywhere.
  </action>
  <verify>
    <automated>grep -q "CONTINUE" docs/Stage-0-Decision-Ledger.md && grep -q "ITERATE" docs/Stage-0-Decision-Ledger.md && grep -q "KILL" docs/Stage-0-Decision-Ledger.md && echo THRESHOLDS_PRESENT</automated>
    <automated>grep -qi "propagation" docs/Stage-0-Decision-Ledger.md && grep -qi "cohort" docs/Stage-0-Decision-Ledger.md && echo DEFINITIONS_PRESENT</automated>
    <automated>node -e "const fs=require('fs');if(fs.readFileSync('docs/Stage-0-Decision-Ledger.md','utf8').includes('—')){process.exit(1)}console.log('NO_LONGDASH')"</automated>
  </verify>
  <done>`docs/Stage-0-Decision-Ledger.md` states the KILL/ITERATE/CONTINUE thresholds, the reading window, the verified-propagation definition, the ledger column schema, and the two-cohort plan, structured to be timestamped before card #1; no forbidden long-dash.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify the hardened maker and the committed ledger</name>
  <action>Pause for the human to confirm the maker validates required fields and consent, handles oversized/HEIC photos gracefully, previews and removes the photo, and the share state Copy link and QR both work for the real link; and that docs/Stage-0-Decision-Ledger.md carries the D-03 thresholds, window, verified-propagation definition, column schema, and both cohorts, with the thresholds timestamped before card #1.</action>
  <what-built>A hardened maker (validation, consent, graceful photo handling, share state with QR and copy-link feedback) and the pre-committed Stage 0 decision ledger doc.</what-built>
  <how-to-verify>
1. Serve locally and open `maker.html`. Submit with empty name/note and no consent: confirm the inline UI-SPEC errors appear and submit is blocked until consent is checked.
2. Upload a large (>10MB) image: confirm the too-big error copy. Upload a normal JPEG: confirm the taped-polaroid preview, then the x remove control and the Photo removed caption.
3. (If you have an iPhone HEIC handy, on a non-Safari browser) upload it: confirm the graceful did-not-go-through / try-a-JPEG error rather than a blank upload.
4. Complete a valid card: in the share state confirm the link field, that Copy link copies and shows Copied for about 1.5s, and that the QR scans on a phone and opens the same /c/<token>/ card. Confirm Make another resets the form and Preview it opens the live card.
5. Open `docs/Stage-0-Decision-Ledger.md`: confirm the KILL/ITERATE/CONTINUE thresholds, the ~30-day/min-10 window, the verified-propagation definition, the column schema, and the two cohorts are all present and unambiguous, and that there is a header slot to timestamp the thresholds before card #1.
6. Create the Google Sheet from the schema and timestamp the thresholds (founder action) before sending any real card.
  </how-to-verify>
  <resume-signal>Type "approved" once the maker is usable end to end and the ledger is committed, or describe what is missing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sender browser to canvas/Storage | The uploaded photo is decoded and re-encoded client-side before it leaves the browser |
| founder process to the ledger | The pre-committed decision rule must be timestamped before card #1 or the gate is not honest |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | undecodable/HEIC photo upload | medium | mitigate | try/catch around createImageBitmap; typed decode error shows the UI-SPEC JPEG-ask copy instead of a blank or broken upload (research Pitfall 3) |
| T-03-02 | Denial of Service | oversized photo upload exhausting free Storage | low | mitigate | 10MB client cap + downscale to ~1600px JPEG before upload (research Don't Hand-Roll) |
| T-03-03 | Repudiation | decision rationalized after the fact | high | mitigate | Ledger thresholds are pre-committed and timestamped before card #1; the decision is read mechanically (D-03) |
| T-03-04 | Information Disclosure | consent for the uploaded photo | medium | mitigate | Required consent checkbox attesting permission to use the photo (Stage 0 spec 5.2); full moderation is the Phase 4 gate |
| T-03-SC | Tampering | esm.sh import of qrcode | low | mitigate | Major-pin `@1`; qrcode is OK in the research audit (mature, 15M+/wk); client-side only |
</threat_model>

<verification>
- `shared/photo.js` imports cleanly and exports `downscalePhoto`.
- The maker blocks submit without consent and shows the UI-SPEC error copy on each failure path.
- The share state Copy link and QR both work for the real `/c/<token>/` link.
- The ledger doc carries the D-03 thresholds, the window, the verified-propagation definition, the column schema, and both cohorts.
- No forbidden long-dash (U+2014) in any new/modified file.
</verification>

<success_criteria>
The maker is usable by real testers (validated input, graceful photo handling, a clean shareable link with QR and copy feedback) and the Stage 0 decision is mechanical and pre-committed (VAL-03 share quality + VAL-04 decision instrument). Combined with Plans 01 and 02, the full create-to-share-to-open-to-make-one-back loop works on free tiers and the analytics funnel fires correctly.
</success_criteria>

<output>
Create `.planning/phases/01-validate-the-loop/01-03-SUMMARY.md` when done. Record the final MAX_EDGE/quality used, the Google Sheet column order as built, and confirmation the thresholds were timestamped before card #1.
</output>

